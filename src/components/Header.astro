---
/**
 * Placeholder during Phase 2 migration.
 * Real editorial top bar lands in P2-8 (EditorialTopBar.astro).
 */
interface Props {
  currentSection?: string;
}
const { currentSection: _ } = Astro.props;
---
<div aria-hidden="true"></div>
