# one format in, another out — identity is rejected
nika: json-to-yaml

tasks:
  translate:
    invoke:
      tool: "nika:convert"
      args:
        input: '{"name":"nika","verbs":4}'
        from: json
        to: yaml

outputs:
  yaml: ${{ tasks.translate.output }}
