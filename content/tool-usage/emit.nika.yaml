# a custom machine event for subscribers — distinct from log
nika: announce-the-ship

tasks:
  announce:
    on_error:
      skip: true
    invoke:
      tool: "nika:emit"
      args:
        event_type: "deploy.finished"
        payload: { env: "prod", ok: true }
